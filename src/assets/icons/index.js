const icons = {
    search: require('~/assets/icons/search.svg').default,
    close: require('~/assets/icons/close.svg').default,
    loading: require('~/assets/icons/loading.svg').default,
};

export default icons;
